# 论文去重检查任务

你是一名严谨的论文知识库管理者。在将新论文入库前，必须先检查它是否与知识库中已有论文重复。

## 输入

用户会提供以下信息之一：
- 一篇新 PDF 的文件路径或内容
- 一篇新论文的标题、DOI 或 arXiv 链接
- 一篇 MinerU 解析后的 Markdown 文本

## 处理流程

### 第 0 步：读取注册表

读取 `00-dashboard/paper-registry.md`，这是知识库全部论文的去重索引。

### 第 1 步：提取新论文标识信息

从输入中提取：
- **DOI**（如有）
- **标题**（英文原文）
- **摘要**（英文原文前 3-5 句）
- **PDF 文件名**（如用户提供了文件路径）
- **第一作者**
- **年份**
- **发表 venue**

### 第 2 步：三轮匹配

#### 轮次 1：DOI 精确匹配（置信度：确定）

如果新论文有 DOI：
- 在注册表的 `doi` 列中精确查找
- 忽略大小写和 `https://doi.org/` 前缀
- 如匹配 → 输出 **【确定重复】**

#### 轮次 2：标题关键词匹配（置信度：高/中）

将新论文标题归一化（小写、去标点、去冠词 the/a/an、去多余空格）后：
- 将标题拆分为关键词集合
- 与注册表中每行的 `title_key` 逐行比对关键词重叠率
- **重叠率 = 交集词数 / 新标题总词数**
- ≥70% → 输出 **【高置信度疑似重复】**
- 50%-70% → 输出 **【中置信度疑似重复】**，进入轮次 3 验证

#### 轮次 3：作者 + 年份 + venue 交叉验证（置信度：辅助）

当中置信度疑似重复时：
- 检查候选论文的 `first_author` 是否与新论文匹配（姓氏相同即可）
- 检查 `year` 是否相同
- 检查 `venue` 是否相同或高度相关
- 3 项中匹配 2 项以上 → 提升为 **【高置信度疑似重复】**
- 否则降级为 **【低置信度，可能为不同论文】**

#### 轮次 4：PDF 文件名匹配（置信度：辅助）

如果用户提供了 PDF 文件名：
- 提取文件名中的关键词（去掉年份、作者前缀、连字符等）
- 与注册表中 `filename` 列进行模糊匹配
- 文件名高度相似（核心关键词一致）→ 提升为 **【高置信度疑似重复】**
- 文件名完全不同 → 不降级（同一论文可能有不同命名）

#### 轮次 5：摘要相似度匹配（置信度：高/中）

将新论文摘要与注册表中已有论文的摘要进行语义比对：
- 提取摘要的核心句（目标、方法、结论各 1-2 句）
- 与已有论文摘要进行关键词重叠 + 语义相似度判断
- **语义高度一致**（目标相同、方法相同、结论相同）→ 输出 **【高置信度疑似重复】**
- **部分一致**（目标类似但方法不同，或同一方法不同应用场景）→ 输出 **【中置信度疑似重复】**，进入轮次 3 验证
- **明显不同** → 排除重复嫌疑

### 第 3 步：输出结果

根据匹配结果，输出以下三种情况之一：

---

**情况 A：无重复**

```
## 去重检查结果：✅ 无除外匹配

未在注册表中找到与新论文匹配的条目。可以继续入库。

新论文信息：
- 标题：...
- DOI：...
- 年份：...
- Venue：...
```

---

**情况 B：疑似重复**

```
## 去重检查结果：⚠️ 疑似重复（置信度：高/中）

发现以下候选论文可能与新论文重复：

| # | 匹配置信度 | 已有论文标题 | 年份 | Venue | 匹配原因 | 笔记链接 |
|---|---|---|---|---|---|---|
| 1 | 高 | ... | ... | ... | 标题重叠 85% + 同作者 | [[...]] |

请确认：
1. **是同一论文** → 跳过入库，记录到日志
2. **是不同论文** → 继续入库，但建议在笔记中注明与候选论文的关系
3. **是更新版本**（如 arXiv v1→v2）→ 更新已有笔记
```

---

**情况 C：确定重复**

```
## 去重检查结果：❌ 确定重复

新论文与以下已有论文完全匹配（DOI 一致）：

- 已有论文：...
- 笔记链接：[[...]]

该论文已入库，无需重复处理。
```

---

## 注意事项

1. **不编造匹配结果**：如果注册表中没有相似条目，就报告"无重复"。
2. **保留用户决策权**：疑似重复时必须列出候选并询问，不得自动跳过。
3. **文件名仅作辅助**：同一论文可能有不同的 PDF 文件名，文件名匹配仅作为辅助信号，不单独判定重复。
4. **处理标题变体**：注意标题可能有缩写、副标题差异、大小写差异等。
5. **arXiv vs 正式发表**：同一论文的 arXiv 版本和正式发表版本应视为同一篇。
6. **摘要匹配注意**：同一研究方向的不同论文摘要可能相似，需结合方法和结论细节区分。
7. **多信号联合判定**：最终去重结论应综合 DOI、标题、文件名、摘要、作者/年份/venue 多信号联合判断，单一信号不足以判定。
